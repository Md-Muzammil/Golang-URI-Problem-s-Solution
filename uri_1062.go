package main

import ("fmt"

"reflect"
)
func main(){

typedef stack si
    
var vet[1005]int

var  argc, char const *argv[]int
{
    var i, curr, n, top int

    while(sc1(n) && n)
    {
    while(sc1(vet[0]) && vet[0])
    {
    fr(i, 1, n)
    sc1(vet[i])

    si p
    curr = 0

    fe(i, 1, n)
    {
    p.push(i)

    while(p.size() > 0 && vet[curr] == p.top())
    {
        curr++
        p.pop()
    }
    }

    (p.size() == 0) ? puts("Yes") : puts("No")
    }

    fmt.Printf("n")
    }


}


}

